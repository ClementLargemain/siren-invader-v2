{
  "name": "sireneinvider",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "clean:data": "rm -rf src/data/tmp && exit 0",
    "start": "pm2 start process.json",
    "stop": "pm2 delete all && npm run clean:data"
  },
  "dependencies": {
    "mongodb": "^4.2.0",
    "mongoose": "^6.0.13",
    "pm2": "^5.1.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.1",
    "eslint": "^8.3.0"
  }
}
